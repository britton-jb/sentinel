<p>Hello <%= user.email %>!</p>

<p>We're contacting you to notify you that an email address change was requested.</p>
<% if !Application.get_env(:sentinel, :confirmable) && Sentinel.RouterHelper.configured? do %>

  <p><a href="<%= Sentinel.RouterHelper.helpers.user_url(Sentinel.RouterHelper.endpoint, :confirm) %>?confirmation_token=<%= confirmation_token %>">Click here to confirm your email address change</a></p>
<% end %>
